/* logic.js */


function minimax(position, depth, isMaximizing){
  score = 0;
  let bestScore = -Infinity;
  if(depth == 0) return score;

  if(isMaximizing){
    let bestScore = -Infinity;
    
  } else {
    let bestScore = Infinity;
  }
}
